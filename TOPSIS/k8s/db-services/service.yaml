apiVersion: v1
kind: Service
metadata:
  name: db-service
  namespace: default
spec:
  selector:
    app: db-service
  ports:
  - name: db-service
    protocol: TCP
    port: 3000
    targetPort: 3000
  type: ClusterIP
